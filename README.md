# Emergency Reporting App (Offline-First PWA)

A Progressive Web App (PWA) for community-based emergency reporting.  
Supports offline reports via Dexie, online sync with Firebase, voting system, and guest/user logging.

---

## 🚀 Features
- Report creation (text + details)
- Offline storage (Dexie.js)
- Firebase sync (online)
- Voting system (valid / troll detection)
- Guest & logged-in users
- Bootstrap UI

---

## 📦 Tech Stack
- React + Vite
- Firebase
- Dexie.js
- Bootstrap / React-Bootstrap

---

## 🛠 Setup
```bash
# Clone repo
git clone <your-repo-url>
cd my-app

# Install dependencies
npm install

# Run locally in ugly ahh Computer
npm run dev

